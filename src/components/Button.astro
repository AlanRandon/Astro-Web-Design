<style>
  @tailwind utilities;

  @layer utilities {
    .btn-arrow:hover path {
      d: path("M 2 12 h 30 m -0.3 0 l -4 4 m 4 -4 l -4 -4");
    }

    .btn-arrow path {
      @apply transition-[d] duration-500;
    }

    .btn-arrow:hover svg {
      @apply mr-4;
    }

    .btn-arrow svg {
      @apply transition-[margin] duration-500;
    }
  }
</style>

<button
  class="block w-fit border-teal-500 border-2 py-2 px-4 rounded-[100vmax] hover:bg-teal-500 hover:text-slate-800 transition-all btn-arrow"
  ><slot /><svg
    xmlns="http://www.w3.org/2000/svg"
    fill="none"
    viewBox="0 0 24 24"
    stroke="currentcolor"
    class="w-6 h-6 inline-block ml-2"
    overflow="visible"
  >
    <path
      d="M 2 12 h 20 m -0.3 0 l -4 4 m 4 -4 l -4 -4"
      stroke-linecap="round"
      stroke-width="2"></path>
  </svg>
</button>
